<!DOCTYPE html>
<html>

<%- include('../components/header.ejs', {title:'Help page - Nature\'s Palette'}) %>

<body class="bg-light">

<%- include('../components/navbar.ejs', {user: user}) %>
<div class="container" style="width: 70% ;margin: auto">
    <br>
    <div class="card bg-white mb-3" style="max-width: 100%;">
        <div class="card-header"><h4>Current search terms</h4></div>
        <div class="card-body">
                <% if (searchTerms) {
                for (let i = 0; i < searchTerms.length; i++) {
                %>
                <label style="">
                    <U><%=searchTerms[i].name %></U>
                </label>
            &nbsp;&nbsp;
                <% } %>
                <% } %>
                <img data-toggle="collapse" href="#add" src="/images/add-24px.svg">
            <HR>
            <div class="collapse col-md-4 multi-collapse" id="add">
                <form action="/addTerm/add" method="post">
                    <div class="form-group">
                        <label for="name">The Name of New Term</label>
                        <input type="text" name="name" class="form-control">
                    </div>
                    <button type="submit" class="btn btn-dark">add</button>
                </form>
            </div>
        </div>
    </div>

                    <%if (typeof insertTerm != 'undefined') { %>

                    <% } %>

</div>
<%- include ../components/scripts.ejs %>

</body>
</html>
